import react from "react";

export const PriceFormatContext = react.createContext("USD");

export const CartContext = react.createContext({});

export const SiteNotificationsContext = react.createContext({});
